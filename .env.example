# Google Sheets Configuration
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here
GOOGLE_SHEET_RANGE=Лист1!A:C

# Server Configuration
PORT=8080
HOST=0.0.0.0
NODE_ENV=production

# Google Cloud Authentication
# When running locally, set this to the path of your service account key file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# When running on Google Cloud Run, authentication is handled automatically
# through the default service account or IAM roles
